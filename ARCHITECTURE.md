# 🏗 Архитектура системы Telegram Gifts Tracker

## 📐 Общая схема

```
┌──────────────────────────────────────────────────────────────┐
│                        FRONTEND                               │
│  ┌─────────────┐     ┌──────────────┐    ┌───────────────┐  │
│  │  index.html │────►│  script.js   │────│ liveUpdates.js│  │
│  └─────────────┘     └──────────────┘    └───────┬───────┘  │
└─────────────────────────────────────────────────│────────────┘
                                                   │
                                          WebSocket│  HTTP API
                                                   │
┌──────────────────────────────────────────────────▼────────────┐
│                        BACKEND                                 │
│  ┌──────────────────────────────────────────────────────┐    │
│  │                    server.js                          │    │
│  │  - Express HTTP server                                │    │
│  │  - WebSocket server (ws)                              │    │
│  │  - Routes initialization                              │    │
│  └───────┬────────────────────────────────┬──────────────┘    │
│          │                                 │                   │
│  ┌───────▼──────────┐            ┌────────▼─────────┐        │
│  │  PriceWatcher    │            │   API Routes     │        │
│  │  ┌────────────┐  │            │  /api/gifts      │        │
│  │  │ Scheduler  │  │            │  /api/gifts/:id  │        │
│  │  │ (10sec)    │  │            │  /api/health     │        │
│  │  └─────┬──────┘  │            └──────────────────┘        │
│  │        │         │                                         │
│  │  ┌─────▼──────┐  │            ┌──────────────────┐        │
│  │  │  Parser    │  │            │  ImageLoader     │        │
│  │  │  (Cheerio) │  │            │  - Download PNG  │        │
│  │  └─────┬──────┘  │            │  - Save local    │        │
│  │        │         │            │  - Update DB     │        │
│  │  ┌─────▼──────┐  │            └────────┬─────────┘        │
│  │  │  Retry     │  │                     │                  │
│  │  │  Logic     │  │                     │                  │
│  │  └─────┬──────┘  │                     │                  │
│  │        │         │                     │                  │
│  └────────┼─────────┘                     │                  │
│           │                               │                  │
│  ┌────────▼───────────────────────────────▼────────┐        │
│  │            DatabaseService (SQLite)              │        │
│  │  ┌────────────┐         ┌──────────────────┐    │        │
│  │  │   gifts    │         │  price_history   │    │        │
│  │  │  - id      │         │  - gift_id       │    │        │
│  │  │  - name    │         │  - old_price     │    │        │
│  │  │  - price   │         │  - new_price     │    │        │
│  │  │  - currency│         │  - change_%      │    │        │
│  │  │  - image   │         │  - timestamp     │    │        │
│  │  └────────────┘         └──────────────────┘    │        │
│  └───────────────────────────────────────────────────┘        │
└────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                     EXTERNAL SOURCES                          │
│  ┌─────────────────────┐      ┌──────────────────────┐      │
│  │  HTML Site Parser   │      │   Image CDN/URLs     │      │
│  │  (blog.thevse.io)   │      │   (PNG downloads)    │      │
│  └─────────────────────┘      └──────────────────────┘      │
└──────────────────────────────────────────────────────────────┘
```

## 🔄 Поток данных

### 1. Инициализация (Startup)
```
1. server.js запускается
2. DatabaseService.init() - создает таблицы
3. PriceWatcher.start() - запускает таймер
4. WebSocket сервер готов к подключениям
5. Express API endpoints готовы
```

### 2. Цикл обновления (каждые 10 сек)
```
1. PriceWatcher.update()
   ↓
2. fetchGifts() - парсит HTML/API
   ↓
3. Для каждого подарка:
   a. db.upsertGift() - сохраняет/обновляет
   b. Проверяет изменение цены
   c. Если цена изменилась → price_history
   d. imageLoader.downloadImage() - скачивает PNG
   ↓
4. Broadcast через WebSocket
   ↓
5. Frontend получает обновление
```

### 3. Frontend подключение
```
1. Браузер открывает index.html
2. liveUpdates.js подключается к WebSocket
3. Получает initial data
4. Подписывается на updates
5. Отображает данные в UI
```

## 🧩 Компоненты

### Backend Сервисы

#### 1. **PriceWatcher** 
```javascript
Задачи:
- Периодический парсинг (configurable interval)
- Retry логика с exponential backoff
- Rate limiting защита
- WebSocket broadcast при изменениях

Ключевые методы:
- start() - запуск таймера
- update() - основной цикл обновления
- fetchGifts() - парсинг источника
- broadcast() - отправка через WS
```

#### 2. **ImageLoader**
```javascript
Задачи:
- Проверка наличия локального файла
- Скачивание PNG с внешнего URL
- Валидация mime-type
- Сохранение в assets/gifts/
- Обновление image_path в БД

Особенности:
- Не перекачивает существующие
- Сохраняет оригинальное качество
- Graceful fail при ошибках
```

#### 3. **DatabaseService**
```javascript
Задачи:
- CRUD операции с промисами
- UPSERT для gifts
- INSERT в price_history
- Queries для API

Таблицы:
- gifts (текущее состояние)
- price_history (изменения)
```

### API Endpoints

```
GET /api/gifts
├─ Возвращает все подарки
└─ Response: { success, data[], count }

GET /api/gifts/:id
├─ Возвращает конкретный подарок
└─ Response: { success, data }

GET /api/gifts/:id/history
├─ История изменения цены
├─ Query: ?limit=100
└─ Response: { success, data[], count }

GET /api/health
└─ Healthcheck endpoint
```

### WebSocket Events

```javascript
// Server → Client
{
  type: 'initial',    // При подключении
  data: Gift[]
}

{
  type: 'update',     // Каждое обновление
  data: Gift[],
  timestamp: number
}
```

## 🔐 Безопасность и ограничения

### Rate Limiting
```javascript
- Exponential backoff: 1s → 2s → 4s → 8s
- MAX_RETRIES: 3 попытки
- REQUEST_TIMEOUT: 10 секунд
- RATE_LIMIT_DELAY: 1 секунда между запросами
```

### User-Agent
```javascript
'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36'
```

### Error Handling
```javascript
try/catch на всех async операциях
Graceful degradation при ошибках
Логирование всех ошибок
Автоматический retry
```

## 📊 Модель данных

### Gift Object
```typescript
interface Gift {
  id: string;              // slug от названия
  name: string;            // "Plush Pepe"
  price: number;           // 50.0
  currency: string;        // "TON" | "Stars" | "USD"
  image_path: string;      // "/assets/gifts/plush-pepe.png"
  source_url: string;      // URL источника
  last_updated: number;    // timestamp
  created_at: number;      // timestamp
}
```

### PriceHistory Object
```typescript
interface PriceHistory {
  id: number;
  gift_id: string;
  old_price: number;
  new_price: number;
  change_percent: number;  // -5.2 или +12.3
  timestamp: number;
}
```

## 🚀 Масштабирование

### Horizontal Scaling
```
┌────────────┐   ┌────────────┐   ┌────────────┐
│  Worker 1  │   │  Worker 2  │   │  Worker 3  │
└─────┬──────┘   └─────┬──────┘   └─────┬──────┘
      │                │                │
      └────────────────┼────────────────┘
                       │
              ┌────────▼─────────┐
              │  Redis PubSub    │
              └────────┬─────────┘
                       │
              ┌────────▼─────────┐
              │  PostgreSQL      │
              └──────────────────┘
```

### Caching Layer
```javascript
┌─────────────┐
│   Client    │
└──────┬──────┘
       │
┌──────▼──────┐
│ Redis Cache │ ← 60 sec TTL
└──────┬──────┘
       │ miss
┌──────▼──────┐
│  Database   │
└─────────────┘
```

### Message Queue (Advanced)
```
PriceWatcher → RabbitMQ → Workers → Database
                  │
                  └──→ ImageLoader Queue
```

## 🔧 Конфигурация для разных источников

### Добавление нового парсера

1. Создать `/backend/parsers/newSource.js`:
```javascript
export class NewSourceParser {
  constructor(url) {
    this.url = url;
  }
  
  async parse($) {
    // Custom parsing logic
    const gifts = [];
    
    $('.product').each((i, elem) => {
      gifts.push({
        id: ...,
        name: ...,
        price: ...,
        currency: ...
      });
    });
    
    return gifts;
  }
}
```

2. Обновить `priceWatcher.js`:
```javascript
import { NewSourceParser } from '../parsers/newSource.js';

const parser = new NewSourceParser(process.env.SOURCE_URL);
const gifts = await parser.parse($);
```

3. Настроить `.env`:
```env
SOURCE_URL=https://new-source.com
PARSER_TYPE=newSource
```

## 📈 Мониторинг

### Логи
```javascript
console.log('🔄 Fetching...')  // Начало обновления
console.log('📦 Found N gifts') // Найдено подарков
console.log('💰 Price change')  // Изменение цены
console.log('✅ Completed')     // Успех
console.error('❌ Error')       // Ошибка
```

### Метрики (можно добавить)
- Количество обновлений/мин
- Среднее время парсинга
- Количество изменений цен
- WebSocket подключений
- API requests/sec

## 🎯 Production Ready Checklist

- [ ] PostgreSQL вместо SQLite
- [ ] Redis для кэша
- [ ] PM2 для process management
- [ ] NGINX reverse proxy
- [ ] HTTPS/SSL
- [ ] Environment variables
- [ ] Health checks
- [ ] Logging (Winston/Bunyan)
- [ ] Error tracking (Sentry)
- [ ] Monitoring (Prometheus/Grafana)
- [ ] Rate limiting (express-rate-limit)
- [ ] Authentication/API keys
- [ ] CORS настройка
- [ ] Docker containerization
- [ ] CI/CD pipeline

## 🔮 Будущие улучшения

1. **Алерты** - уведомления при резких изменениях цен
2. **Графики** - визуализация истории цен
3. **Telegram Bot** - бот для уведомлений
4. **Multiple sources** - парсинг нескольких сайтов
5. **ML Predictions** - предсказание цен
6. **Admin Panel** - управление через веб-интерфейс
